version: 1.0.{build}
image: Visual Studio 2017
init:
- pwsh: Dockerfile.test.build.sh
before_build:
- pwsh: >-
    docker-compose -f docker-compose.test.yml up -d

    timeout 60
build:
  verbosity: minimal
